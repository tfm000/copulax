[pytest]
pythonpath = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    golden: golden regression tests (deselect with '-m "not golden"')
    local_only: tests that only run locally (deselect with '-m "not local_only"')